# Инструкция по запуску приложения

## 📋 Предварительные требования

1. **Node.js** (версия 18 или выше)
   - Скачайте с [nodejs.org](https://nodejs.org/)
   - Проверьте установку: `node --version`

2. **npm** или **yarn**
   - Обычно устанавливается вместе с Node.js
   - Проверьте: `npm --version`

3. **Expo CLI** (опционально, но рекомендуется)
   ```bash
   npm install -g expo-cli
   ```

4. **Для запуска на устройстве:**
   - **Android**: Установите [Expo Go](https://play.google.com/store/apps/details?id=host.exp.exponent) из Google Play
   - **iOS**: Установите [Expo Go](https://apps.apple.com/app/expo-go/id982107779) из App Store

## 🚀 Шаги по запуску

### 1. Установите зависимости

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит все необходимые пакеты (может занять несколько минут).

### 2. Запустите приложение

После установки зависимостей выполните:

```bash
npm start
```

или

```bash
expo start
```

### 3. Выберите платформу

После запуска вы увидите QR-код и меню. Выберите один из вариантов:

- **Для Android**: Нажмите `a` или отсканируйте QR-код в приложении Expo Go
- **Для iOS**: Нажмите `i` или отсканируйте QR-код в приложении Expo Go
- **Для веб-браузера**: Нажмите `w`

### 4. Альтернативные команды запуска

```bash
# Запуск для Android
npm run android

# Запуск для iOS (только на macOS)
npm run ios

# Запуск в веб-браузере
npm run web
```

## 🔧 Решение проблем

### Ошибка: "Cannot find module"

```bash
# Удалите node_modules и package-lock.json, затем переустановите
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "Metro bundler error"

```bash
# Очистите кеш Metro
npx expo start --clear
```

### Ошибка с NativeWind

Убедитесь, что файл `global.css` существует и импортирован в `app/_layout.tsx`.

### Проблемы с TypeScript

```bash
# Проверьте версию TypeScript
npx tsc --version

# Если нужно, обновите
npm install --save-dev typescript@latest
```

## 📱 Первый запуск

1. После запуска приложение откроется на вашем устройстве/эмуляторе
2. Создайте первый кошелек через кнопку "New Wallet" на главном экране
3. Добавьте транзакции в кошелек
4. Просматривайте аналитику на вкладке "Analytics"

## 🎯 Полезные команды

- `r` - Перезагрузить приложение
- `m` - Открыть меню разработчика
- `j` - Открыть отладчик
- `Ctrl+C` - Остановить сервер разработки

## 📝 Примечания

- Приложение работает полностью офлайн (данные хранятся локально)
- Курсы валют загружаются из API НБУ при наличии интернета
- При отсутствии интернета используются резервные курсы валют

